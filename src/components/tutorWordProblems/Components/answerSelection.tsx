
import { Box, Card} from "@chakra-ui/react"
import type { selectAnswersType } from "../types"

export const AnswerSelection = (info: selectAnswersType)=>{

  return(
    <Card>
      
    </Card>
  )
}