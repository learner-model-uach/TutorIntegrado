import { ColorModeButton} from "./ui/color-mode";

export const DarkModeToggle = () => {
  return (
    <ColorModeButton
      // size="lg"
      rounded="full"
      // fontSize="2xl"
      color="gray.100"
      aria-label="Dark mode toggle"
    />
  );
};
